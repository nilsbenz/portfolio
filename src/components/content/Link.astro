---
import { cn } from "@/lib/utils";
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {}
---

<a
  {...Astro.props}
  class={cn(
    "text-primary focus-visible:ring-ring/50 ease-out-expo rounded font-semibold underline underline-offset-2 transition-shadow duration-700 focus:outline-none focus-visible:ring-3",
    Astro.props.class,
  )}><slot /></a
>
